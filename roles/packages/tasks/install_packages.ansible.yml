---

- name: Install specified packages on Fedora with progress display
  ansible.builtin.dnf:
    name: "{{ item }}"
    state: present
  loop: "{{ packages_packages_to_install | default([]) }}"
  loop_control:
    label: "{{ item }}"
  when: (packages_packages_to_install | default([])) | length > 0
