---
# main task file for flatpak

- name: Setup Flatpak remotes
  ansible.builtin.include_tasks: setup_flatpak_remotes.ansible.yml

- name: Include tasks to install flatpak
  ansible.builtin.include_tasks: install_flatpak.ansible.yml
  # Removed the 'when' condition since install_flatpak.ansible.yml has its own condition
  # to check if flatpak_packages_to_install has content

# No explicit OS check here. Flatpak is generally cross-distribution,
# but the DNF role implies a Fedora context. If broader OS support is needed
# in the future for Flatpak, a distribution check would be added here.
# For now, assuming environment where Flatpak is expected to work.
